# 构建产物：可由 tools/build_release.py 重建（安装包 2.6 MB、zip 300 KB，不进库）
dist/

# Windows 编译产物（可重编；请注意三个 WebView2 DLL 与 ArborInversa.ico 必须进库，否则编不出来）
win-app/*.exe
*.pdb
*.WebView2/

# Python 缓存
__pycache__/
*.pyc

# 运行日志与临时文件
serve.log
*.log
*.tmp
*~

# 系统 / 编辑器垃圾
.DS_Store
Thumbs.db
desktop.ini

# 个人内容一律不进库（仓库只放框架：README ＋ 前端 ＋ 后端 ＋ 工具 ＋ 框架自带的示例树）
data.before-*.json
data.backup.json
media/*
# 例外：示例树要用的插图（框架自带的演示素材）必须进库
# 注意命名：示例-框架示意.svg 已按新约定改名成它的「值」尾豏魚賄尾.svg
# （值＝图片内容算出来的五言，见 index.html 里的 IMG_TABLE；改名后正文引用也要跟着改）
!media/尾豏魚賄尾.svg
!media/示例-枝叶结构.svg

# 本地运维/AI 交接笔记：给新对话看的碎碎念，只在本机，不进仓库
开发笔记.md

# 说明：仓库的 data.json ＝ 由 示例树.txt 生成的示例树（框架自带、非个人内容），
#       用 tools/check_data.js 核对一致性。你的内容正本在应用目录 C:\ArborInversa\，
#       给别人看内容用随包的《美术史树-试用.txt》。
