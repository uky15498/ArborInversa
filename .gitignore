# 构建产物：可由 tools/build_release.py 重建（安装包 2.6 MB、zip 300 KB，不进库）
dist/

# Windows 编译产物（可重编；请注意三个 WebView2 DLL 与 ArborInversa.ico 必须进库，否则编不出来）
win-app/*.exe
*.pdb
*.WebView2/

# Python 缓存
__pycache__/
*.pyc

# 运行日志与临时文件
serve.log
*.log
*.tmp
*~

# 系统 / 编辑器垃圾
.DS_Store
Thumbs.db
desktop.ini

# 内容文件：仓库只放框架，任何内容数据都不进库
data.before-*.json
data.backup.json
media/

# 说明：本仓库＝框架（README ＋ 前端 ＋ 后端 ＋ 工具 ＋ 空框架树 data.json）。
#       你的内容正本在应用目录 C:\\ArborInversa\\ ，给别人看内容用随包的《美术史树-试用.txt》。
